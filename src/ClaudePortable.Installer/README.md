# ClaudePortable.Installer

Placeholder for spec Phase 6. Will host:

- WiX 4 `.wxs` source that builds `ClaudePortable-x.y.z.msi`
- Optional autostart / Start-menu shortcut
- .NET 10 Desktop Runtime pre-req check + bootstrapper
- SignTool-based code signing (certificate path from an env var, never committed)

Tracked as [issue #3](../../../issues).
